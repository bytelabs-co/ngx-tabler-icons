on:
  workflow_dispatch:

env:
  GH_TOKEN: ${{ github.token }}  

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4.2.2
    - run: |        
        gh variable set --env repository TEST_VAR --body "Set from workflow"
