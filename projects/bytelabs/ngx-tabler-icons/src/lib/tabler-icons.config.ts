export interface TablerIconsConfig {
  size: string;
  color: string;
  stroke: string;
}

export const defaultConfig: TablerIconsConfig = {
  size: "24px",
  color: "currentColor",
  stroke: "2px"
};